name: CI — PR checks
on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  quick-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Quick health check
        run: |
          echo "CI PR check running"
          # If you later want to run tests, add commands here (pytest, flake8, etc.)
